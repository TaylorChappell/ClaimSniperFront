# Production: map this subdomain to the Railway backend service.
# Keeping frontend + API under claimsniper.fun makes HttpOnly session cookies
# first-party/same-site and avoids mobile Safari blocking railway.app cookies.
VITE_API_URL=https://api.claimsniper.fun
